<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/dialogToggleSource"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:text="@string/dialog_source"
            android:textAllCaps="false"
            app:backgroundTint="@color/ios_surface"
            app:cornerRadius="12dp"
            app:strokeWidth="1dp"
            app:strokeColor="@color/ios_accent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/dialogTogglePreview"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="@string/dialog_preview"
            android:textAllCaps="false"
            app:backgroundTint="@color/ios_surface"
            app:cornerRadius="12dp"
            app:strokeWidth="1dp"
            app:strokeColor="@color/ios_stroke" />

    </LinearLayout>

    <EditText
        android:id="@+id/dialogEditText"
        android:layout_width="match_parent"
        android:layout_height="320dp"
        android:layout_marginTop="12dp"
        android:background="@drawable/bg_ios_card"
        android:gravity="top|start"
        android:inputType="textMultiLine"
        android:padding="12dp"
        android:textColor="@color/ios_text_primary"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/dialogPreviewText"
        android:layout_width="match_parent"
        android:layout_height="320dp"
        android:layout_marginTop="12dp"
        android:background="@drawable/bg_ios_card"
        android:gravity="top|start"
        android:padding="12dp"
        android:textColor="@color/ios_text_primary"
        android:textSize="14sp"
        android:visibility="gone" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/dialogCopy"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="12dp"
        android:text="@string/btn_copy"
        android:textAllCaps="false"
        app:backgroundTint="@color/ios_accent"
        app:cornerRadius="14dp" />
</LinearLayout>
